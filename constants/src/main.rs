fn main() {
    // type and value of a constant cant change in the program and cannot redefine the constants
    const SECONDS_IN_MINUTES: u32= 60;
    println!("{} seconds in a minute",SECONDS_IN_MINUTES);
}
